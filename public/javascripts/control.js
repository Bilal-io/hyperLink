var elfBitly = {
    initialize: function() {
        'use strict';
        elfLog.setLevel(elfLog.logLevelSilent); // Done once per application
        elfLog.log(elfLog.logLevelDetails, 'ElfDisplay.Render index: ' + index);
    }
};
